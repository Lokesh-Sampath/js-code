const accountId = 1174  // when we use const we will be not able to change the value after declaring it once
let accountEmail = "loki@gmail.com"   // one of the most used type for declaring the varaibles
var accountPassword = "29749274" // prefer not to use var, because issue in block and functional scope
accountCity = "Chennai"  // we can declare some varaible just by their name, not using any keyword
let accountState; // undefined varaible
console.table([accountId,accountEmail,accountPassword,accountCity,accountState]); // table used to print the op in table form

